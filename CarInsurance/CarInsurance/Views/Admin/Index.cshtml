@using CarInsurance.ViewModels
@model List<QuoteFormVm>

<div class="row no-gutters m-3">
    <div class="card col-sm-12 col-md-10 col-lg-6 mx-auto shoadow">
        <div class="card-header text-center">
            <h5 class="m-0">Quote</h5>
        </div>
        <div class="card-body">
            @foreach (var quote in Model)
            {
                <input type="hidden" name="Id" value="@quote.Id" />
                <div class="row mb-3">
                    <div class="col-4">
                        @quote.FirstName
                    </div>
                    <div class="col-3">
                        @quote.LastName
                    </div>
                    <div class="col-5">
                        @quote.EmailAddress
                    </div>
                </div>
            }
        </div>
    </div>
</div>